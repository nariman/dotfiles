# List of templates from gitignore.io
# This content should be replaced by the following templates

# Command to execute (WILL OVERWRITE THE FILE!):
# cat ~/.gitignore_global | grep -o '^[^#]*' | paste -d, | curl -L -s https://www.gitignore.io/api/$(cat -) > ~/.gitignore_global

# OS
linux
macos
windows

# Editors
androidstudio
intellij
phpstorm
pycharm
sublimetext
vim
visualstudiocode
webstorm

# Languages / Platforms
android
c
c++
clojure
cuda
elixir
erlang
go
java
kotlin
node
python
r
ruby
rust
scala

# Frameworks
angular
django
flask
laravel
rails

# Language tools
virtualenv

# Tools
database

# Other

archive
archives
compressed
compressedarchive
compression
